# Firebase Admin SDK (Server-side)
# Get these from your Firebase project's service account JSON
# Project Settings > Service Accounts > Generate New Private Key

FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"

# Firebase Client SDK (Client-side - exposed to browser)
# Get these from your Firebase project settings > General > Your apps > Web app config

PUBLIC_FIREBASE_API_KEY=your-api-key
PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
PUBLIC_FIREBASE_PROJECT_ID=your-project-id

# Authorized user emails (comma-separated)
# Users with these emails can access the app after authenticating with Firebase Auth
AUTHORIZED_EMAILS=user1@example.com,user2@example.com
